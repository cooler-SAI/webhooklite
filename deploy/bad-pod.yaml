apiVersion: v1
kind: Pod
metadata:
  name: hacker-pod
  namespace: default
  labels:
    app: bad-app
spec:
  containers:
    - name: alpine
      image: alpine:latest
      command: ["sleep", "3600"]
      securityContext:
        runAsUser: 0